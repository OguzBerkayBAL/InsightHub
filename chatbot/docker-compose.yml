version: '3'

services:
  redis:
    image: redis:latest
    container_name: adaptiverag-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning

volumes:
  redis-data:
    driver: local
